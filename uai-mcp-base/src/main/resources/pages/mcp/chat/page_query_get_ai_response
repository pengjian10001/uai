
<#assign
	sessionId = sessionId!t.randomUniqueId()
	clientName = clientName!'test-client'
	ucid = ucid!'0'
	queryParam = {
		"clientName": clientName,
		"sessionId" : sessionId?string,
		"ucid": ucid
	}
	result=mcpcli.query(message, queryParam)
/>
<#--mcpcli.query返回的是字符串，直接输出，无需t.toJson(result)-->
${result}
